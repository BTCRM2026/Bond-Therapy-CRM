import { NextResponse } from "next/server";
import { PORTAL_HEADER, portalFromRequest } from "@/lib/portal";
const API = process.env.API_INTERNAL_URL ?? "http://localhost:3001";
export async function PATCH(request: Request, context: { params: Promise<{ id: string }> }) { try { const portal = portalFromRequest(request); if (!portal || portal === "DISTRIBUTOR") return NextResponse.json({ message: "Invoices are not available." }, { status: 403 }); const { id } = await context.params; const upstream = await fetch(`${API}/billing/invoices/${encodeURIComponent(id)}/status`, { method: "PATCH", headers: { "content-type": "application/json", cookie: request.headers.get("cookie") ?? "", [PORTAL_HEADER]: portal }, body: await request.text(), cache: "no-store" }); return new NextResponse(await upstream.text(), { status: upstream.status, headers: { "content-type": "application/json" } }); } catch { return NextResponse.json({ message: "Invoice service is temporarily unavailable." }, { status: 503 }); } }
